# 자바 프로그래밍 심화

## 학습 목표
- 자료구조(Collection Framework)의 동작 원리와 제네릭을 통한 타입 안정성 확보
- 람다식(Lambda)과 스트림(Stream) API를 활용한 현대적 함수형 패러다임 구현
- 견고하고 우아한 예외 처리(Exception Handling)의 이해와 구현
- 파일 시스템 제어를 위한 바이트 및 문자 기반 입출력 스트림 활용법 습득
- 멀티스레드 기반 동시성 제어 및 임계 영역 동기화 기법 이해
- TCP 및 UDP 소켓 통신과 멀티스레드를 결합한 다중 접속 서버 설계 능력 배양
- 테스트 주도 개발(TDD)의 철학 체득과 단위 테스트(JUnit) 코드 작성 능력 배양

## 목차
- [1. 예외 처리(Exception)](#1-예외-처리exception)
    + [1.1 에러와 예외](#11-에러와-예외)
    + [1.2 예외 클래스의 계층 구조](#12-예외-클래스의-계층-구조)
    + [1.3 try-catch-finally 블록과 자원 관리](#13-try-catch-finally-블록과-자원-관리)
    + [1.4 throws와 throw](#14-throws와-throw)
    + [1.5 사용자 정의 예외](#15-사용자-정의-예외)
- [2. 자료 구조와 컬렉션 프레임워크(Collection)](#2-자료-구조와-컬렉션-프레임워크collection)
    + [2.1 컬렉션 프레임워크 구조](#21-컬렉션-프레임워크-구조)
    + [2.2 List 인터페이스(ArrayList, LinkedList)](#22-list-인터페이스arraylist-linkedlist)
    + [2.3 Set 인터페이스(HashSet, TreeSet)](#23-set-인터페이스hashset-treeset)
    + [2.4 Map 인터페이스(HashMap, TreeMap)](#24-map-인터페이스hashmap-treemap)
    + [2.5 제네릭(Generic)](#25-제네릭generic)
- [3. 모던 자바 - 람다(Lambda)와 함수형 프로그래밍](#3-모던-자바---람다lambda와-함수형-프로그래밍)
    + [3.1 함수형 프로그래밍 패러다임](#31-함수형-프로그래밍-패러다임)
    + [3.2 람다식(Lambda Expression) 문법](#32-람다식lambda-expression-문법)
    + [3.3 표준 함수형 인터페이스](#33-표준-함수형-인터페이스)
    + [3.4 메서드 참조(Method Reference)](#34-메서드-참조method-reference)
    + [3.5 어노테이션(Annotation)](#35-어노테이션annotation)
- [4. 자바 입출력(I/O)](#4-자바-입출력io)
    + [4.1 입출력 스트림 개요](#41-입출력-스트림-개요)
    + [4.2 바이트 기반과 문자 기반 스트림](#42-바이트-기반과-문자-기반-스트림)
    + [4.3 보조 스트림](#43-보조-스트림)
- [5. 멀티스레드(Multi-Thread)](#5-멀티스레드multi-thread)
    + [5.1 프로세스와 스레드](#51-프로세스와-스레드)
    + [5.2 스레드 생성과 실행](#52-스레드-생성과-실행)
    + [5.3 스레드 동기화](#53-스레드-동기화)
    + [5.4 스레드 안전성 확보](#54-스레드-안전성-확보)
- [6. 자바 네트워크 프로그래밍(Network)](#6-자바-네트워크-프로그래밍network)
    + [6.1 네트워크 기초](#61-네트워크-기초)
    + [6.2 TCP 소켓 통신](#62-tcp-소켓-통신)
    + [6.3 UDP 소켓 통신](#63-udp-소켓-통신)
    + [6.4 멀티스레드 다중 접속 서버](#64-멀티스레드-다중-접속-서버)
- [7. 데이터 가공 - 스트림(Stream) API](#7-데이터-가공---스트림stream-api)
    + [7.1 스트림 API 개념](#71-스트림-api-개념)
    + [7.2 스트림 파이프라인 3단계](#72-스트림-파이프라인-3단계)
    + [7.3 주요 중간 연산](#73-주요-중간-연산)
    + [7.4 주요 최종 연산](#74-주요-최종-연산)
    + [7.5 지연 연산(Lazy Evaluation)과 루프 퓨전(Loop Fusion)](#75-지연-연산lazy-evaluation과-루프-퓨전loop-fusion)
- [8. 소프트웨어 품질과 TDD 철학](#8-소프트웨어-품질과-tdd-철학)
    + [8.1 단위 테스트(Unit Test)](#81-단위-테스트unit-test)
    + [8.2 JUnit 프레임워크 기초](#82-junit-프레임워크-기초)
    + [8.3 TDD 3단계 사이클](#83-tdd-3단계-사이클)
    + [8.4 TDD를 도입하는 이유와 아키텍처](#84-tdd를-도입하는-이유와-아키텍처)

---

# 1. 예외 처리(Exception)

## 1.1 에러와 예외
- 에러(Error)
    - 시스템 수준에서 발생하는 복구 불가능한 심각한 문제
    - 메모리 부족(OutOfMemoryError)이나 스택 오버플로우(StackOverflowError) 등 개발자가 코드로 수습할 수 없음
- 예외(Exception)
    - 애플리케이션 수준에서 발생하며 프로그램 코드로 수습 가능한 비교적 가벼운 오류
    - 예외 처리를 통해 프로그램의 비정상 종료를 막고 실행 상태를 유지 가능

- 💻 실습 (Exception과 Error): [ch01/ErrorTest.java](../workspace/04_advanced/ch01/ErrorTest.java)

## 1.2 예외 클래스의 계층 구조
- 모든 예외와 에러의 최상위 클래스는 [Throwable](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/Throwable.html) 클래스
- Throwable 하위에 시스템 에러를 나타내는 [Error](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/Error.html)와 애플리케이션 예외를 나타내는 [Exception](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/Exception.html) 클래스가 존재
- Exception 클래스 계층 구조는 크게 두 가지로 분류됨
    - Checked Exception (일반 예외)
        - [RuntimeException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/RuntimeException.html) 계층을 제외한 모든 Exception 하위 클래스
        - 컴파일러가 예외 처리를 강제하며, 처리를 하지 않으면 컴파일 에러 발생
        - 주로 외부 자원과의 연동 등 프로그램 외부 요인으로 발생 (예: [IOException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/io/IOException.html), [SQLException](https://docs.oracle.com/en/java/javase/25/docs/api/java.sql/java/sql/SQLException.html), [FileNotFoundException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/io/FileNotFoundException.html))
    - Unchecked Exception (실행 예외)
        - [RuntimeException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/RuntimeException.html)과 그 하위 클래스
        - 컴파일러가 예외 처리를 강제하지 않음
        - 주로 개발자의 코드 로직 오류나 부주의로 발생 (예: [NullPointerException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/NullPointerException.html), [ArithmeticException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/ArithmeticException.html), [IndexOutOfBoundsException](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/IndexOutOfBoundsException.html))

- Checked vs Unchecked 비교 표

| 구분 | Checked Exception | Unchecked Exception |
| --- | --- | --- |
| 예외 처리 여부 | 반드시 처리해야 함 (강제) | 명시적인 처리를 강제하지 않음 |
| 확인 시점 | 컴파일 시점 | 실행 시점 (Runtime) |
| 대표 클래스 | IOException, SQLException | NullPointerException, IndexOutOfBoundsException |

- 💻 실습 (Checked와 Unchecked 예외 예제): [ch01/CheckedUnchecked.java](../workspace/04_advanced/ch01/CheckedUnchecked.java)

## 1.3 try-catch-finally 블록과 자원 관리
- try-catch-finally 동작 구조
    - try 블록: 예외가 발생할 가능성이 있는 코드를 작성하는 영역
    - catch 블록: 특정 예외가 발생했을 때 처리할 코드를 작성하며, 여러 개를 선언할 수 있음
    - finally 블록: 예외 발생 여부와 상관없이 항상 실행되며 주로 입출력 스트림, 데이터베이스 커넥션 등 자원 해제에 사용함
    - try-catch-finally 예시
      ```java
      try{
          int num2 = str.length(); // str이 null일 경우 NullPointerException 발생
          int result = num1 / num2; // str이 ""일 경우 ArithmeticException 발생
          System.out.println(num1 + " / " + num2 + " = " + result);
      }catch(ArithmeticException e){
          System.out.println("0으로 나눌 수 없습니다.");
      }catch(NullPointerException e){
          System.out.println("str이 null입니다.");
      }finally{
          System.out.println("finally는 try-catch 블럭이 실행된 뒤 항상 실행을 보장.");
      }
      ```

    - 다형성을 이용한 예외 처리
        - catch 블록에 상위 예외 클래스를 지정하면, 그 하위 예외 클래스의 인스턴스들도 모두 다형성에 의해 함께 잡을 수 있음
        - 예를 들어, catch(Exception e) 로 지정하면 모든 하위 예외를 일괄 처리할 수 있음
        - 단, 다중 catch 블록을 구성할 때 상위 예외 클래스를 하위 예외 클래스보다 먼저 선언하면 아래의 하위 catch 블록은 도달할 수 없는 코드가 되어 컴파일 에러가 발생하므로, 반드시 하위 예외 클래스부터 순차적으로 배치해야 함
        - 다형성 활용 예시
          ```java
          // 올바른 예시: 하위 예외를 먼저 catch하고 상위 예외를 나중에 catch함
          try {
              int num2 = str.length();
              int result = num1 / num2;
              System.out.println(result);
          } catch (NullPointerException e) {
              System.out.println("Null 예외 발생");
          } catch (Exception e) {
              System.out.println("그 외의 일반 예외 발생");
          }
    
          // 잘못된 예시: 상위 예외를 먼저 catch하면 하위 catch 블록에서 컴파일 에러 발생
          try {
              int num2 = str.length();
              int result = num1 / num2;
              System.out.println(result);
          } catch (Exception e) {
              System.out.println("모든 예외 처리");
          } catch (NullPointerException e) { // 컴파일 에러: 도달할 수 없는 코드(Unreachable catch block)
              System.out.println("Null 예외 발생");
          }
          ```

- 멀티 캐치(Multi-catch) 문법
    - 자바 7부터 지원하며 한 catch 블록에서 여러 예외를 파이프(|) 기호로 연결하여 동시에 처리함
    - catch(ArithmeticException | ArrayIndexOutOfBoundsException e)
    - 멀티 캐치 예시
      ```java
      try{
          int num2 = str.length(); // str이 null일 경우 NullPointerException 발생
          int result = num1 / num2; // str이 ""일 경우 ArithmeticException 발생
          System.out.println(num1 + " / " + num2 + " = " + result);
      }catch(ArithmeticException | NullPointerException e){
          System.out.println(e.getMessage());
      }
      ```

- 💻 실습 (try-catch-finally): [ch01/TryCatchTest.java](../workspace/04_advanced/ch01/TryCatchTest.java)
- 💻 실습 (Multi-catch): [ch01/MultiCatchTest.java](../workspace/04_advanced/ch01/MultiCatchTest.java)

- try-with-resources 문법 (자원 자동 해제)
    - 자바 7에서 도입된 자원 자동 반납 문법
    - [AutoCloseable](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/AutoCloseable.html) 인터페이스를 구현한 자원 객체를 try 괄호 안에 선언하면 블록 종료 시 자동으로 close() 메서드가 호출됨
    - 기존 finally 블록에서 자원을 해제할 때 발생하던 복잡한 예외 처리와 누수 문제를 방지함
    - try-with-resources 사용전 예시
      ```java
      static void insert(int memberId, String title, String content){
          String sql = "INSERT INTO post (member_id, title, content) VALUES ("+memberId+", '"+title+"', '"+content+"')";
  
          // finally에서 접근 가능하도록 try 블럭 외부에서 선언
          Connection conn = null;
          Statement stmt = null;
          try{
              conn = DriverManager.getConnection(DB_URL, DB_USER, DB_PASSWORD);
              stmt = conn.createStatement();
              int affectedRows = stmt.executeUpdate(sql);
              System.out.println("게시글 등록 완료: " + affectedRows + "건 반영됨.");
  
          }catch(Exception e){
              System.out.println("에러 발생: " + e.getMessage());
          }finally{
              // 생성된 리소스를 생성의 역순으로 해제
              try{ if(stmt != null) stmt.close(); } catch (Exception e){ }
              try{ if(conn != null) conn.close(); } catch (Exception e){ }
          }
      }
      ```

    - try-with-resources 사용후 예시
      ```java
      static void insert(int memberId, String title, String content){
          String sql = "INSERT INTO post (member_id, title, content) VALUES ("+memberId+", '"+title+"', '"+content+"')";
          try(Connection conn = DriverManager.getConnection(DB_URL, DB_USER, DB_PASSWORD);
              Statement stmt = conn.createStatement()){
              int affectedRows = stmt.executeUpdate(sql);
              System.out.println("게시글 등록 완료: " + affectedRows + "건 반영됨.");
          }catch(Exception e){
              System.out.println("에러 발생: " + e.getMessage());
          }
      }
      ```

- 💻 실습 (try-with-resources): [ch01/JdbcMemberTest.java](../workspace/04_advanced/ch01/JdbcMemberTest.java)

## 1.4 throws와 throw
- throw (예외를 발생시키는 행위)
    - 개발자가 로직 내부에서 직접 예외 객체를 생성하여 명시적으로 던질 때 사용함
    - `throw new IllegalArgumentException(메시지)`
    - 주요 사용 시점 (어떤 상황에서 예외를 던지는가):
        - 전달받은 아규먼트가 비즈니스 규칙에 위배되어 즉각적인 예외를 생성해야 할 때 (예: 회원가입 시 나이 필드에 음수가 들어왔을 때)
        - 현재 프로그램의 비즈니스 로직상 흐름 진행이 불가능할 때 (예: 계좌 잔액보다 많은 금액을 출금 요청했을 때)
        - 시스템 예외(SQLException 등)를 비즈니스적 의미를 담은 사용자 정의 예외로 재포장하여 던지고자 할 때
- throws (예외 발생 가능성을 외부에 선언하는 행위)
    - 메서드 선언부에 작성하며, 해당 메서드 내부에서 발생할 가능성이 있는 예외 목록을 호출자에게 명시하고 예외 처리를 상위로 전가할 때 사용함
    - `public void check() throws IOException`
    - 주요 사용 시점 (어떤 상황에서 예외를 메서드 밖으로 넘기는가):
        - 발생한 예외를 현재 메서드 내에서 try-catch로 해결(데이터 복구나 롤백 등)할 수 없어 호출자에게 해당 대처 방법을 떠넘겨야 할 때
        - Checked Exception이 발생하는 API나 라이브러리를 메서드 내부에서 호출하는 경우, 컴파일러를 통해 호출자 측에서 이 예외를 처리하도록 강제하고 싶을 때
    - 무분별한 throws Exception 사용의 문제점
        - 구체적인 예외 타입을 명시하지 않고 `throws Exception`을 남발하면 어떤 예외가 발생하는지 파악하기 어렵고, 호출하는 측에서 유의미한 예외 처리를 수행할 수 없음
        - 호출 측에 불필요한 예외 처리 의무를 떠넘기는 안티 패턴[^1]이므로 반드시 발생할 수 있는 구체적인 예외들을 명시해야 함
- 예외 재발생(Exception Re-throwing)
    - catch 블록에서 예외를 처리한 후 다시 throw를 통해 예외를 발생시켜 호출한 곳으로도 예외 사실을 전달하는 방식

## 1.5 사용자 정의 예외
- 사용자 정의 예외 선언
    - 비즈니스 요구사항에 맞는 명확한 이름의 예외 클래스를 직접 정의하여 사용함
    - `Exception` 또는 `RuntimeException`을 상속받아 구현하며 일반적으로 에러 메시지와 에러 코드를 포함시킴
- 현대 자바 예외 처리의 트렌드
    - 예외 처리를 강제하여 코드가 복잡해지는 Checked Exception보다는, 유연한 예외 처리가 가능한 Unchecked Exception(RuntimeException)을 상속받아 정의하는 것이 권장됨
    - 스프링 프레임워크를 포함한 현대적 프레임워크들의 대다수 예외도 RuntimeException을 기반으로 작동함
    - 무분별한 사용자 정의 예외 선언을 지양하고 자바의 표준 예외(IllegalArgumentException, IllegalStateException 등)를 우선적으로 재사용하는 것을 권장함

---

# 2. 자료 구조와 컬렉션 프레임워크(Collection)

## 2.1 컬렉션 프레임워크 구조
- 컬렉션 프레임워크 정의
    - 자바에서 데이터를 효율적으로 저장하고 관리할 수 있도록 표준화하여 제공하는 자료구조 및 알고리즘의 집합체
    - 표준화된 인터페이스를 통해 일관된 방식으로 데이터를 처리할 수 있도록 지원
- 핵심 인터페이스 계층 구조
    - Collection 인터페이스: List, Set, Queue 등의 상위 규격 역할을 담당
    - Map 인터페이스: Key-Value 구조로 데이터를 보관하므로 Collection 인터페이스와 별개의 구조로 설계
- 주요 표준 API 명세 문서: [Collection](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/Collection.html), [List](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/List.html), [Set](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/Set.html), [Map](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/Map.html)

```java
import java.util.*;

public class CollectionOverviewTest {
    public static void main(String[] args) {
        // 인터페이스 다형성을 이용한 컬렉션 생성 예시
        Collection<String> list = new ArrayList<>();
        Collection<String> set = new HashSet<>();

        list.add("Data1");
        set.add("Data1");
    }
}
```

## 2.2 List 인터페이스(ArrayList, LinkedList)
- 특징: 순서를 유지하며 데이터의 중복 저장을 허용
- ArrayList
    - 특징: 내부적으로 크기 조절이 가능한 배열 구조를 사용
    - 장점: 인덱스를 통한 데이터 조회 속도가 매우 신속
    - 단점: 리스트 크기 확장 시 배열 복사가 일어나거나, 중간에 데이터를 삽입/삭제할 때 기존 데이터들을 뒤로 밀어내거나 앞으로 당기는 이동 연산이 추가되어 성능이 저하
- LinkedList
    - 특징: 이중 연결 리스트 구조로 각 노드가 이전 노드와 다음 노드의 참조 주소를 보유
    - 장점: 중간에 데이터를 삽입하거나 제거할 때 링크 연결 정보만 변경하므로 신속한 연산 속도 제공
    - 단점: 특정 인덱스의 요소를 찾기 위해 처음 또는 끝부터 링크를 타고 순차적으로 조회해야 하므로 임의 검색 성능이 떨어지는 편
- 구현체 비교 분석
  | 항목 | ArrayList | LinkedList |
  | --- | --- | --- |
  | 내부 구조 | 가변 배열 | 이중 연결 리스트 |
  | 인덱스 조회 | 신속 | 탐색 필요 |
  | 순차 삽입/삭제 | 신속 | 신속 |
  | 중간 삽입/삭제 | 데이터 이동 필요 | 주소 탐색 필요 |
  | 메모리 사용량 | 적음 (데이터만 저장) | 많음 (참조 주소 보관용 메모리 추가 필요) |

```java
// List 사용 방법 및 중간 삽입 예시 (부분 코드)
List<String> arrayList = new ArrayList<>();
arrayList.add("Java");
arrayList.add("Spring");
arrayList.add(1, "React"); // 1번 인덱스에 삽입하여 기존 요소를 뒤로 밀어냄

List<String> linkedList = new LinkedList<>();
linkedList.add("Docker");
linkedList.add(0, "AWS"); // 0번 인덱스에 삽입하여 링크 관계 재구성
```

## 2.3 Set 인터페이스(HashSet, TreeSet)
- 특징: 데이터의 저장 순서를 보장하지 않으며 중복 저장 불가
- HashSet
    - 특징: 해시 테이블 구조를 사용하여 데이터를 보관
    - 장점: 요소의 추가, 삭제, 검색 성능이 데이터 양에 관계없이 매우 일정하고 신속
    - 주의사항: 저장하려는 커스텀 객체의 중복 판단을 수행하기 위해 equals()와 hashCode() 메서드 정의 필수
- TreeSet
    - 특징: 이중 검색 트리 (Red-Black Tree) 구조를 기반으로 설계
    - 장점: 추가하는 데이터가 자동으로 오름차순 정렬되어 보관
    - 단점: 데이터가 들어올 때마다 트리 구조 재조정이 일어나므로 HashSet에 비해 삽입/삭제 성능이 제한적
    - 주의사항: 저장하려는 객체가 Comparable 인터페이스를 상속받아 구현했거나 별도의 Comparator를 생성 시 인자로 전달 필수
- 구현체 비교 분석
  | 항목 | HashSet | TreeSet |
  | --- | --- | --- |
  | 내부 자료구조 | 해시 테이블 | 이진 정렬 트리 (Red-Black Tree) |
  | 정렬 특징 | 순서 없음 (정렬 없음) | 정렬 있음 (기본 오름차순) |
  | 성능 | 상대적으로 매우 신속 | 정렬을 동반하므로 상대적으로 느린 편 |
  | 객체 비교 필수 조건 | hashCode() 및 equals() 재정의 | Comparable 인터페이스 구현 또는 Comparator 주입 |

```java
// Set 사용 방법 및 중복 제거 예시 (부분 코드)
Set<String> set = new HashSet<>();
set.add("Apple");
set.add("Apple"); // 중복 요소 삽입 시도가 무시되어 최종 크기는 1로 처리

// 커스텀 객체 중복 제거를 위한 필수 정의 구조 (부분 코드)
static class Member {
    private String name;
    private int age;

    @Override
    public boolean equals(Object o) {
        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;
        Member member = (Member) o;
        return age == member.age && Objects.equals(name, member.name);
    }

    @Override
    public int hashCode() {
        return Objects.hash(name, age);
    }
}
```

## 2.4 Map 인터페이스(HashMap, TreeMap)
- 특징: Key와 Value가 한 쌍(Entry)을 이루는 방식으로 데이터를 저장하며 Key는 중복 불가, Value는 중복을 허용
- HashMap
    - 특징: 해시 함수를 통해 Key의 저장 위치를 결정하므로 정렬 순서가 보장되지 않음
    - 장점: 추가, 삭제, 검색 속도가 대단히 신속
- TreeMap
    - 특징: 이진 정렬 트리 기반으로 동작하여 Key 값을 기준으로 자동 오름차순 정렬 상태를 유지
    - 장점: 범위 검색이나 정렬된 리스트를 반환할 때 적합
    - 단점: 요소를 추가할 때 정렬 연산이 동반되어 HashMap 대비 성능에 차이 존재
- Map의 데이터 탐색 기법
    - keySet(): 전체 Key 정보를 Set 형태로 반환받아 순회함. 순회 루프 안에서 Value를 꺼내기 위해 get() 메서드를 반복 호출하므로 비효율이 발생할 여지 존재
    - entrySet(): Key와 Value가 결합된 Entry 정보를 Set 형태로 가져와 순회함. 추가적인 조회 없이 Key와 Value를 한 번에 다룰 수 있어 대량 데이터를 순회할 때 권장
- 구현체 비교 분석
  | 항목 | HashMap | TreeMap |
  | --- | --- | --- |
  | 내부 자료구조 | 해시 테이블 | 이진 정렬 트리 (Red-Black Tree) |
  | 정렬 기준 | 순서 보장 없음 | Key 기준 자동 오름차순 정렬 |
  | Null 허용 | Key 및 Value 모두 Null 지정 허용 | Key로 Null 지정 불가 |

```java
// Map 사용 방법 및 Entry 순회 예시 (부분 코드)
Map<String, Integer> map = new HashMap<>();
map.put("사과", 1000);
map.put("사과", 1500); // 동일한 Key를 넣으면 기존 Value가 덮어쓰기 처리

// 권장되는 entrySet() 순회 기법 (부분 코드)
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println("과일: " + entry.getKey() + ", 가격: " + entry.getValue());
}
```

## 2.5 제네릭(Generic)
- 제네릭의 개념
    - 클래스나 메서드 내부에서 사용되는 데이터 타입을 컴파일 단계에서 외부로부터 지정받도록 지원하는 메커니즘
    - 불필요한 타입 강제 변환 작업을 생략할 수 있게 도와주며 컴파일 수준에서 강도 높은 타입 검증을 구현
- 타입 파라미터 약속 기호
    - T: Type (타입 지칭)
    - E: Element (요소, List 등의 자료구조 객체 저장 타겟)
    - K: Key (식별 키)
    - V: Value (보관 값)
- 제네릭 메서드 선언
    - 클래스 제네릭과 별개로 메서드 호출 순간에만 매개변수와 반환 타입을 타입 파라미터와 동기화
    - 선언 시 제한 지시자나 반환 타입 앞에 `<E>`와 같이 타입 파라미터 영역을 정의
- 와일드카드와 PECS(Producer Extends, Consumer Super)
    - 와일드카드 기호인 `?`는 정해지지 않은 자유로운 데이터 유형을 수렴할 때 선언
    - 상한 제한 와일드카드 `? extends T`
        - T를 포함한 하위 타입들만 대입할 수 있도록 범위를 고정
        - 주로 내부 값을 꺼내서 읽고 처리할 때 유용하며 데이터를 안전하게 상위 타입인 T로 다룰 수 있게 설계
    - 하한 제한 와일드카드 `? super T`
        - T를 포함한 상위 타입들만 대입할 수 있도록 범위를 고정
        - 주로 해당 컬렉션에 T 혹은 T의 하위 객체를 안전하게 새로 추가하여 사용할 때 적합

```java
// 제네릭 클래스 정의 예시 (부분 코드)
public class Box<T> {
    private T content;
    public T getContent() { return content; }
    public void setContent(T content) { this.content = content; }
}

// PECS 적용 예시 (부분 코드)
// extends를 통한 조회 성능 안전성 보장 (Animal의 하위 리스트 접근)
public static void printAnimals(List<? extends Animal> list) {
    for (Animal animal : list) {
        System.out.println("동물: " + animal.getName());
    }
}

// super를 통한 데이터 보존 안전성 보장 (Cat의 상위 리스트에 추가)
public static void addCat(List<? super Cat> list) {
    list.add(new Cat("길고양이"));
}
```

---

# 3. 모던 자바 - 람다(Lambda)와 함수형 프로그래밍

## 3.1 함수형 프로그래밍 패러다임
- 함수형 프로그래밍 개요
    - 애플리케이션의 상태 변화를 유발하지 않고 오직 순수 함수만을 결합하여 신뢰성 높은 결과물을 생성하는 개발 패러다임
    - 기존 객체 지향 프로그래밍과 달리 함수 자체를 변수에 대입하거나 다른 함수의 인자로 전달하고 반환값으로 수령하는 일급 객체[^2] (First-class Object) 취급 방식 활용
- 순수 함수(Pure Function) 조건
    - 입력 데이터가 일치하면 언제나 동일한 출력 데이터를 보장하여 예측 가능성을 확보
    - 함수 동작 실행 중 외부 시스템의 전역 변수나 상태 값을 수정하는 부작용(Side Effect)을 원천 차단

```java
public class PureFunctionTest {
    // 동일한 입력값에 대해 항상 동일한 연산 결과를 반환하는 순수 함수
    public static int add(int a, int b) {
        return a + b;
    }
}
```

- 💻 실습 (람다 기본 및 캡처링): [ch03/LambdaTest.java](../workspace/04_advanced/ch03/LambdaTest.java)
- 💻 실습 (표준 함수형 인터페이스): [ch03/FunctionalInterfaceTest.java](../workspace/04_advanced/ch03/FunctionalInterfaceTest.java)
- 💻 실습 (메서드 참조 문법): [ch03/MethodReferenceTest.java](../workspace/04_advanced/ch03/MethodReferenceTest.java)
- 💻 실습 (어노테이션 메타데이터): [ch03/AnnotationTest.java](../workspace/04_advanced/ch03/AnnotationTest.java)

## 3.2 람다식(Lambda Expression) 문법
- 정의: 익명 함수(Anonymous Function)를 표기하기 위해 반환 타입과 메서드 명칭을 완전히 제거하고 화살표 기호 `->`를 사용하여 구현하는 문법 규칙
- 람다식 표기 형식
    - `(매개변수) -> { 실행문; }`
    - 단일 실행 명령문일 경우 중괄호 `{}`와 세미콜론 `;` 생략 가능
- 람다 캡처링과 로컬 변수 제약 사항
    - 람다식 내부에서 외부 범위에 속한 메서드 내 지역 변수를 참조할 때, 해당 변수는 명시적으로 final 상수로 선언되어 있거나 대입 연산이 추가 발생하지 않는 사실상 final(effectively final) 상태에 도달해 있어야 사용 가능
    - 지역 변수는 스택(Stack) 메모리 영역에 생성되므로 스레드가 종료되면 소멸되는데, 람다의 생명 주기는 다를 수 있어 자바 런타임이 해당 지역 변수를 복사(Copy)하여 독립적으로 소장
    - 복사된 데이터와 원본의 일관성을 컴파일러가 강력히 수호하기 위해 변동이 일체 불가한 final 제약을 준수하도록 강제 설계

```java
// 기존 무명/익명 클래스 선언 기법 (부분 코드)
Runnable run1 = new Runnable() {
    @Override
    public void run() {
        System.out.println("기존 익명 객체 구동");
    }
};

// 람다 적용으로 불필요한 보일러플레이트 제거 (부분 코드)
Runnable run2 = () -> System.out.println("람다 변환 구동");
```

## 3.3 표준 함수형 인터페이스
- 정의: 함수형 프로그래밍을 돕기 위해 자바 표준 라이브러리 패키지인 [java.util.function](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/function/package-summary.html) 내부에서 미리 범용 프로토타입으로 제안한 인터페이스 계층
- 핵심 표준 어댑터 비교
  | 분류 | 매개변수 종류 | 반환 값 형식 | 주요 가동 메서드 명칭 | 핵심 목표 |
  | --- | --- | --- | --- | --- |
  | Predicate | T (단일 객체) | boolean | test(T t) | 입력된 객체의 유효성 검사 및 통과 필터링 |
  | Consumer | T (단일 객체) | 없음 (void) | accept(T t) | 입력값을 소모하여 가공 결과를 화면 출력 등 처리 |
  | Function | T (변환 타겟) | R (변환 완료) | apply(T t) | 입력 데이터를 특정 포맷이나 다른 유형으로 매핑 |
  | Supplier | 없음 | T (신규 생성) | get() | 지연 로딩 방식을 통한 신규 데이터 조달 및 리턴 |

```java
// 표준 함수형 인터페이스 응용 문법 (부분 코드)
Predicate<Integer> checkLimit = num -> num > 100;
System.out.println("제한 초과 판단: " + checkLimit.test(150));

Consumer<String> writeLog = msg -> System.out.println("로그 누적: " + msg);
writeLog.accept("이벤트 발생");
```

## 3.4 메서드 참조(Method Reference)
- 정의: 람다식이 매개변수를 전혀 수정하지 않고 기존 다른 클래스의 단일 메서드만을 호출할 때, 코드 중복을 정리하여 간결하게 압축 표기하는 기법
- 문법 기호: `타겟_클래스_혹은_인스턴스::메서드명`
- 참조 형식 구분
    - 정적 메서드 참조: `Class::staticMethod`
    - 임의 객체의 인스턴스 메서드 참조: `Class::instanceMethod` (람다 파라미터가 호출 주체로 동작)
    - 특정 객체의 인스턴스 메서드 참조: `instance::instanceMethod`
    - 생성자 호출 참조: `Class::new`

```java
// 람다 기본 형태 (부분 코드)
Function<String, Integer> lambdaWay = s -> Integer.parseInt(s);

// 메서드 참조 기법으로 축소 구현 (부분 코드)
Function<String, Integer> refWay = Integer::parseInt;
```

## 3.5 어노테이션(Annotation)
- 어노테이션의 역할
    - 자바 소스코드 컴파일 시점이나 클래스 배포, 애플리케이션 가동 시점에 코드에 메타데이터를 밀착시켜 특정 정보를 런타임에 리플렉션[^3] 기술로 파싱하거나 컴파일 룰 지시 목적으로 주입하는 특수 문법
- 필수 주요 표준 어노테이션
    - @Override: 해당 메서드가 부모 타입의 멤버를 알맞게 덮어쓰기 구현했는지 컴파일 단계에서 강도 높게 유효성 검증 수행
    - @Deprecated: 안전성이나 아키텍처 상의 이유로 사용 자제를 유도하며, 적용 지점을 호출할 시 경고 출력 유발
    - @FunctionalInterface: 클래스가 오직 하나의 추상 메서드만을 보유한 유효한 함수형 인터페이스인지 검증하며 규칙 위배 시 컴파일 차단
- 메타 어노테이션 및 사용자 정의 어노테이션
    - 어노테이션을 생성하기 위해 대상 어노테이션 상단에 선언하여 속성을 제한하는 부모 어노테이션
    - @Target: 클래스, 메서드, 필드 중 어떤 부분에 어노테이션을 적용할 수 있는지 대상 범위를 확정
    - @Retention: 어노테이션 정보의 보존 생명 주기 결정 (SOURCE: 컴파일 후 즉시 소멸, CLASS: 클래스 파일까지 수록되나 런타임에 소멸, RUNTIME: 실행 시점 리플렉션[^3]<sup>2</sup> 지원)

```java
// 표준 어노테이션 및 커스텀 어노테이션 정의 기법 (부분 코드)
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface ExecutionCount {
  int value() default 1; // 단일 속성 값 보관
}
```

---

# 4. 자바 입출력(I/O)

## 4.1 입출력 스트림 개요
- 스트림(Stream)의 정의
    - 컴퓨터 내부의 프로그램과 외부 장치(파일, 네트워크 등) 사이에 데이터를 송수신할 수 있도록 마련된 단방향 통로 구조
    - 입력 장치로부터 데이터를 읽어 들이는 입력 스트림과 데이터를 외부로 송출하는 출력 스트림으로 완전 분리되어 독립 작동
- 자바 표준 I/O 주요 명세 문서: [InputStream](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/io/InputStream.html), [Reader](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/io/Reader.html)

## 4.2 바이트 기반과 문자 기반 스트림
- 바이트 기반 스트림
    - 1byte 단위로 데이터를 처리하며 그림, 동영상, 압축 파일 등 모든 형태의 바이너리 데이터를 안전하게 보관 및 복사
    - 최상위 클래스: InputStream / OutputStream
- 문자 기반 스트림
    - 2byte(또는 유니코드 가변 단위) 크기의 텍스트 데이터를 문자 단위로 처리하여 전 세계 모든 텍스트를 인코딩 깨짐 없이 제어
    - 최상위 클래스: Reader / Writer
- 스트림 분류 비교
  | 분류 | 바이트 기반 스트림 | 문자 기반 스트림 |
  | --- | --- | --- |
  | 최상위 추상 클래스 | InputStream, OutputStream | Reader, Writer |
  | 주요 데이터 타겟 | 이미지, 동영상, 실행 파일 등 바이너리 | 텍스트 문서, 설정 파일 등 일반 문자 |
  | 단위 리더기 예시 | FileInputStream, FileOutputStream | FileReader, FileWriter |

```java
// 파일 쓰기 및 읽기 예시 (부분 코드)
try (FileWriter fw = new FileWriter("sample.txt")) {
    fw.write("자바 I/O 실습");
} catch (IOException e) {
    System.err.println("오류 감지: " + e.getMessage());
}
```

## 4.3 보조 스트림
- 보조 스트림의 개념
    - 스스로 파일이나 네트워크에 직접 빨대를 꽂아 입출력을 수행할 수 없으나, 기존의 1차 스트림에 붙어 성능을 보강하거나 추가 편의 기능을 제공하는 데 기여
- 대표적인 보조 스트림 유형
    - 성능 향상 버퍼 스트림: BufferedInputStream / BufferedOutputStream, BufferedReader / BufferedWriter (내부 메모리 버퍼 공간을 두어 물리적인 디스크 I/O 횟수를 절감하고 성능을 수십 배 향상)
    - 문자 변환 스트림: InputStreamReader / OutputStreamWriter (바이트 스트림을 문자 스트림으로 전환하는 젠더 가교 역할)

```java
// 버퍼 보조 스트림을 사용한 효율적인 파일 읽기 (부분 코드)
try (BufferedReader br = new BufferedReader(new FileReader("sample.txt"))) {
    String line = br.readLine();
} catch (IOException e) {
    System.err.println("에러 발생: " + e.getMessage());
}
```

- 💻 실습 (파일 입출력과 보조 스트림): [ch05/IoTest.java](../workspace/04_advanced/ch05/IoTest.java)

---

# 5. 멀티스레드(Multi-Thread)

## 5.1 프로세스와 스레드
- 프로세스(Process)의 개념
    - 운영체제로부터 독립적인 메모리 및 필요한 자원을 할당받아 컴퓨터 실행 컨텍스트에 등록되어 구동 중인 단일 프로그램을 의미
- 스레드(Thread)의 개념
    - 하나의 프로세스 내부에서 실행 흐름의 가장 작고 기초적인 단위를 뜻하며, 메모리 중 Stack 영역을 제외한 Code, Data, Heap 공간을 공유하여 협력
    - 표준 API 명세 문서: [Thread](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/lang/Thread.html)

## 5.2 스레드 생성과 실행
- Thread 클래스를 직접 상속하는 방식
    - Thread 클래스를 부모로 삼아 run() 메서드를 오버라이딩하여 스레드가 실행할 코드를 명시하며, 다중 상속 제약이 발생하는 취약점이 노출
- Runnable 인터페이스를 구현하는 방식 (권장)
    - 인터페이스만 가져오므로 클래스 확장 한계를 극복하고 리액티브하게 태스크 단위로 설계가 가능하여 결합도가 낮은 안전 구조 달성

```java
// Thread 상속을 통한 구동 (부분 코드)
class MyThread extends Thread {
    public void run() { System.out.println("동작"); }
}
new MyThread().start();

// Runnable 구현을 통한 구동 (부분 코드)
Runnable runTask = () -> System.out.println("동작");
new Thread(runTask).start();
```

## 5.3 스레드 동기화
- 동기화(Synchronization)의 필요성
    - 여러 스레드가 동시에 공유 자원에 접근하여 쓰기 처리를 진행할 때 데이터가 꼬이거나 망가지는 레이스 컨디션[^4] (Race Condition)이 나타나 데이터 불일치 발생
- synchronized 키워드 활용
    - 특정 개체나 메서드에 락(Lock)을 설정하여, 오직 키를 얻은 유일한 스레드만 임계 영역(Critical Section) 내부 코드를 통과할 수 있도록 강제 제한

```java
// 메서드 단위 동기화 기법 (부분 코드)
public synchronized void deposit(int amount) {
    balance += amount; // 한 번에 한 스레드만 접근을 허가하여 안전성 확보
}
```

## 5.4 스레드 안전성 확보
- 스레드 안전(Thread-Safe)
    - 여러 스레드가 동시에 접근하여 작업을 수행하더라도 문제없이 항상 예측 가능한 정상 데이터를 도출하는 성질
- 안전성 확보 전략
    - 무상태성 설계: 전역 변수를 최대한 배제하고 메서드 내부 지역 변수만을 활용하여 동시성 마찰을 사전 회피
    - 불변성(Immutable) 활용: 한 번 선언되면 절대 수정 불가능한 객체(final 지시어 등 활용)를 공유하여 스레드가 상태를 변경할 틈을 차단
    - 스레드 안전 컬렉션 적용: Vector나 Hashtable과 같이 모든 동작에 락이 걸려 성능이 저하되는 구조 대신, ConcurrentHashMap이나 CopyOnWriteArrayList 등 정교하게 튜닝된 락 구조를 갖춘 컬렉션을 활용

- 💻 실습 (스레드 동기화와 임계 영역): [ch06/ThreadTest.java](../workspace/04_advanced/ch06/ThreadTest.java)

---

# 6. 자바 네트워크 프로그래밍(Network)

## 6.1 네트워크 기초
- IP 주소와 포트 번호
    - IP: 네트워크 환경에서 고유한 컴퓨터 위치 식별용 주소
    - Port: 컴퓨터 내부의 어떤 프로세스가 데이터 패킷을 수령해야 하는지 정확한 어플리케이션 목적지를 결정하는 고유 식별 번호 (0 ~ 65535 범위 분배)
- 소켓(Socket) 통신 개요
    - 서로 멀리 떨어진 컴퓨터 사이의 프로세스 간 양방향 게이트웨이 역할을 하는 인터페이스 규격

## 6.2 TCP 소켓 통신
- TCP(Transmission Control Protocol)의 특징
    - 연결 지향성 통신 프로토콜로 3-Way Handshake를 거쳐 논리적 가상 통로를 개설한 후 신뢰도 높은 패킷 제어 보장 (흐름 제어 및 오류 탐색 복구 내장)
- ServerSocket과 Socket 객체 역할
    - ServerSocket: 지정 포트에서 대기(Listen)를 하다가 클라이언트 접속 시 연결 전용 Socket 인스턴스를 동적으로 생성 및 분배
    - Socket: 서버와의 세부 스트림 입출력 송수신 통신을 실무 담당
    - 표준 API 명세 문서: [ServerSocket](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/net/ServerSocket.html)

```java
// TCP 소켓 연동 기본 구성 (부분 코드)
try (ServerSocket server = new ServerSocket(50000)) {
  Socket client = server.accept(); // 클라이언트 접속 시까지 블로킹 대기
} catch (IOException e) {
  System.err.println("서버 에러: " + e.getMessage());
}
```

## 6.3 UDP 소켓 통신
- UDP(User Datagram Protocol)의 특징
    - 연결 설정 절차를 밟지 않고 상대방 수신 여부를 검증하지 않은 채 무차별적으로 데이터를 던지는 패킷 지향 프로토콜 (DatagramPacket 사용)
    - 장점: 통신 비용이 지극히 낮고 응답 속도가 빨라 실시간 스트리밍 중계에 제격
    - 단점: 데이터 분실이나 패킷 전송 순서 뒤바뀜이 생겨도 자체 복구 불가능

## 6.4 멀티스레드 다중 접속 서버
- 단일 스레드 접속 처리 서버의 한계
    - 서버 소켓이 1명의 클라이언트 연결을 수령하여 I/O를 수행 중인 경우, 다른 클라이언트들의 연결은 대기열에 계속 블로킹되어 서비스 불능 상태에 도달
- 멀티스레딩 및 가상 스레드 활용 다중화 구조
    - 클라이언트가 accept()를 통과하여 연결을 생성하면, 즉시 새 실행 스레드를 생성하여 개별 소켓 연산을 이관하고 메인 스레드는 다시 accept 대기로 복귀하여 다중 접속 분산 처리 달성

- 💻 실습 (소켓 통신 에코 서버): [ch07/NetworkTest.java](../workspace/04_advanced/ch07/NetworkTest.java)

---

# 7. 데이터 가공 - 스트림(Stream) API

## 7.1 스트림 API 개념
- 스트림의 정의
    - 다양한 데이터 소스(컬렉션, 배열 등)를 추상화하여 일관된 방식으로 다룰 수 있도록 지원하는 기능
    - 기존의 for 루프나 Iterator를 사용한 외부 반복자 방식 대신, 컬렉션 내부에서 연산을 알아서 처리하는 내부 반복자 방식을 사용하여 가독성을 개선
    - 표준 API 명세 문서: [Stream](https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/stream/Stream.html)
- 스트림의 핵심 특징
    - 데이터 원본 보존: 원본 데이터를 직접 변경하지 않고 읽기 전용으로 가공하여 새로운 결과물 반환
    - 일회용 소멸 구조: Iterator와 유사하게 한 번 사용되어 최종 연산이 끝나면 스트림이 닫혀 재사용 불가
    - 선언형 코드 구현: 무엇을 수행할지 가독성 높은 코드로 기술하여 비즈니스 로직에만 집중 가능

```java
import java.util.*;

public class StreamConceptTest {
  public static void main(String[] args) {
    List<String> list = Arrays.asList("a", "b", "c");
    // 스트림을 이용한 내부 반복 기반 선언형 출력
    list.stream().forEach(System.out::println);
  }
}
```

- 💻 실습 (스트림 기본 파이프라인): [ch04/StreamPipelineTest.java](../workspace/04_advanced/ch04/StreamPipelineTest.java)
- 💻 실습 (주요 중간 연산 활용): [ch04/StreamIntermediateTest.java](../workspace/04_advanced/ch04/StreamIntermediateTest.java)
- 💻 실습 (주요 최종 연산 기법): [ch04/StreamTerminalTest.java](../workspace/04_advanced/ch04/StreamTerminalTest.java)
- 💻 실습 (지연 연산 및 루프 퓨전): [ch04/StreamLazyEvaluationTest.java](../workspace/04_advanced/ch04/StreamLazyEvaluationTest.java)

## 7.2 스트림 파이프라인 3단계
- 스트림이 최종 결과물을 도출하기 위해 여러 연산을 파이프 형태로 연결하는 흐름 구조
- 단계별 동작 설명
    1. 스트림 생성: 컬렉션, 배열, 파일 등의 데이터 소스로부터 스트림 인스턴스를 마련
    2. 중간 연산: 데이터를 변환, 필터링, 정렬 등을 수행하며 반환값이 다시 스트림 형태를 유지하여 여러 개를 체이닝 연계 가능
    3. 최종 연산: 전체 스트림 요소를 소모하며 통계치 집계, 리스트 수집, 콘솔 출력 등을 통해 결과를 도출하고 스트림 파이프라인을 종료

```java
import java.util.*;

public class StreamPipelineTest {
    public static void main(String[] args) {
        List<Integer> list = Arrays.asList(1, 2, 3, 4, 5);

        // 1단계(생성) -> 2단계(중간 연산 filter) -> 3단계(최종 연산 count) 연동
        long count = list.stream()
                         .filter(n -> n > 2)
                         .count();

        System.out.println("3보다 큰 수의 개수: " + count); // 3
    }
}
```

## 7.3 주요 중간 연산
- 스트림의 가공 과정을 담당하며 결과를 다시 스트림으로 돌려주는 메서드
- 중간 연산 카테고리 요약
  | 중간 연산 메서드 | 기능 및 역할 설명 |
  | --- | --- |
  | filter(Predicate) | 조건식에 부합하는 데이터만 선택적으로 통과 처리 |
  | map(Function) | 스트림 요소를 가공하여 다른 형태의 값으로 변환 매핑 |
  | flatMap(Function) | 중첩된 컬렉션 구조를 1차원의 평탄화된 단일 스트림으로 재구성 |
  | sorted() / sorted(Comparator) | 요소를 오름차순 또는 지정된 기준에 맞춰 정렬 배치 |
  | distinct() | 요소 간의 중복을 배제하여 단일 값들만 추출 |
  | limit(long) / skip(long) | 스트림 요소의 개수를 제한하거나 앞부분 데이터 배제 처리 |

```java
// 복합적인 중간 연산 구현 방식 (부분 코드)
List<String> list = Arrays.asList("a", "b", "a", "c", "b");
list.stream()
    .distinct() // 중복 데이터 배제
    .sorted()   // 기본 순서 정렬
    .map(String::toUpperCase) // 대문자 치환
    .forEach(System.out::println); // A, B, C 출력
```

## 7.4 주요 최종 연산
- 파이프라인 of 종착역에 해당하며 데이터 처리를 매듭짓고 스트림 객체를 소멸시키는 메서드
- 최종 연산 카테고리 요약
  | 최종 연산 메서드 | 기능 및 역할 설명 |
  | --- | --- |
  | forEach(Consumer) | 각 요소를 순회하며 지정된 동작을 실행 (출력 등) |
  | collect(Collector) | 요소를 가공하여 List, Set, Map 등의 자료구조로 가공 수집 |
  | reduce(BinaryOperator) | 요소를 하나씩 줄여가며 연산을 누적 수행하여 단일 결과 도출 |
  | count() / sum() / average() | 요소의 총합, 평균값 등 다양한 수치 통계 집계 연산 |
  | anyMatch() / allMatch() / noneMatch() | 조건식에 일부 또는 전체가 부합하는지 여부 검증 (boolean 리턴) |

```java
// collect 수집 연산 활용 방식 (부분 코드)
List<String> names = Arrays.asList("Kim", "Park", "Lee", "Kang", "Koo");
List<String> result = names.stream()
                           .filter(name -> name.startsWith("K"))
                           .map(String::toUpperCase)
                           .collect(Collectors.toList()); // 리스트 변환 수집
```

## 7.5 지연 연산(Lazy Evaluation)과 루프 퓨전(Loop Fusion)
- 지연 연산 메커니즘
    - 스트림 파이프라인에서 최종 연산이 호출되기 직전까지는 중간 연산이 실제로 수행되지 않고 예약을 마친 상태로 대기
    - 데이터의 전체 조회를 미루다가 반드시 결과가 필요한 시점에만 계산을 진행하므로 메모리 및 성능 효율 개선
- 루프 퓨전 동작
    - 여러 단계의 중간 연산을 개별적으로 전체 반복하는 방식이 아님
    - 각 데이터 요소 단위로 필터, 매핑 등의 전체 파이프라인 라인을 수직으로 관통하며 한꺼번에 연산을 병합하여 처리하는 현상

```java
// 지연 연산 및 루프 퓨전 관측 (부분 코드)
List<Integer> list = Arrays.asList(1, 2, 3);
list.stream()
    .filter(n -> {
        System.out.println("filter 체크: " + n);
        return n > 1;
    }); // 최종 연산이 누락되어 콘솔 출력이 전혀 발생하지 않는 구조
```

---

# 8. 소프트웨어 품질과 TDD 철학

## 8.1 단위 테스트(Unit Test)
- 정의: 프로그램 동작 검증을 위한 최소 기본 단위(일반적으로 개별 메서드 수준)를 격리하여 테스트 시나리오를 구성하고 오류 여부를 확인하는 과정
- 단위 테스트 작성 이유
    - 디버깅 비용을 극적으로 낮추고, 코드 변경이나 리팩토링 진행 시 기존 기능이 오작동하지 않는지 안전장치로 기여

```java
// 단위 테스트 로직 예시 (수동 검증 부분 코드)
Calculator calc = new Calculator();
int result = calc.add(10, 20);
if (result == 30) {
  System.out.println("테스트 합격");
}
```

## 8.2 JUnit 프레임워크 기초
- JUnit 개요
    - 자바 생태계에서 가장 널리 도입된 업계 표준 단위 테스트 도구
    - 어노테이션 기법(@Test, @BeforeEach, @AfterEach 등)을 이용해 테스트 흐름을 정의하고 Assertions 단언 함수들로 예측 값 부합 여부를 검사

```java
// JUnit 5 기본 테스트 구성 예시 (부분 코드)
class CalculatorTest {
    @Test
    void addTest() {
        Calculator calc = new Calculator();
        assertEquals(5, calc.add(2, 3));
    }
}
```

## 8.3 TDD 3단계 사이클
- TDD(Test Driven Development) 정의
    - 실제 구동 코드를 개발하기에 앞서, 실패 시나리오를 지닌 테스트 코드를 선행 작성하여 설계를 구체화하고 유도해가는 방법론
- TDD 가동 사이클 과정
    1. RED 단계: 가동 가능한 테스트를 제일 먼저 빌드 (구현체가 아직 없어 반드시 실패를 유발)
    2. GREEN 단계: 실패 테스트를 간신히 넘기기 위해 최소 수준의 코드로 조속히 구현 및 통과 완료
    3. REFACTOR 단계: 중복이 나돌거나 가독성이 저하된 부분들을 리팩토링하여 구조를 보강하며, 기 작성된 테스트의 통과 상태를 유지

```java
// TDD GREEN 단계를 완료한 최소 코드의 구조 (부분 코드)
class Greeter {
    public String sayHello(String name) {
        return "Hello, " + name; // GREEN 통과만을 위해 하드코딩 형태로 구현
    }
}
```

## 8.4 TDD를 도입하는 이유와 아키텍처
- TDD 도입의 이점과 객체지향 설계 유도
    - 테스트가 용이한 코드로 깎아가는 과정에서, 클래스 사이에 의존 관계가 단단히 굳어지는 하드코딩이 붕괴되고, 인터페이스 중심 설계와 의존성 주입[^5] (Dependency Injection)이 자연스럽게 유도
    - 결국 느슨한 결합(Loose Coupling)과 뚜렷한 응집도(High Cohesion)를 지닌 유지보수가 용이한 모범 아키텍처 구축

- 💻 실습 (단위 테스트와 validator 설계): [ch08/TddTest.java](../workspace/04_advanced/ch08/TddTest.java)

---

[^1]: 소프트웨어 개발에서 자주 발생하지만 비효율적이거나 해로운 결과를 초래하는 잘못된 설계나 코딩 패턴
[^2]: 컴퓨터 프로그래밍에서 다른 객체들에 적용 가능한 연산을 모두 지원하는 객체를 뜻하며, 함수를 매개변수로 전달하거나 반환값으로 사용하는 등 변수처럼 자유롭게 다룰 수 있음을 의미함
[^3]: 실행 중인 자바 프로그램이 자기 자신을 검사하거나 내부 클래스 정보, 메서드 등의 메타데이터를 동적으로 분석하고 수정할 수 있게 돕는 기법
[^4]: 둘 이상의 스레드가 공유 자원에 동시에 접근하여 값을 변경하려고 경쟁할 때, 실행 순서에 따라 결과가 달라져 데이터 일관성이 깨지는 현상
[^5]: 객체지향 설계에서 필요한 의존 객체를 직접 생성하지 않고, 생성자나 세터를 통해 외부에서 공급받아 객체 간 결합도를 낮추는 기법